# Codex Pet Dock 产品设计

> 让 Codex 额度一眼可见。

## 1. 产品定义

**产品名：Codex Pet Dock**

中文描述：Codex 宠物用量底座  
英文描述：A glanceable usage dock for the Codex desktop pet.

Codex Pet Dock 是一个独立的 Windows 伴随工具。它让官方 Codex 宠物站在
一个可交互的状态底座上，用最少的信息回答：

1. 当前最紧张的官方额度还剩多少？
2. 额度什么时候重置？
3. 本周本机大约处理了多少 Token？
4. 数据是否仍在正常刷新？

它不是 Codex 皮肤、宠物替换器、账单系统或完整数据分析平台。

## 2. 用户问题

### 核心问题

Codex 桌面端的额度入口较深。用户需要点击个人入口，再进入剩余额度页面，
无法在连续工作时快速判断“今天还能不能继续跑大任务”。

### 次级问题

- 官方额度百分比与本机 Token 活动是两种口径，容易混淆。
- 重置时间比“用了多少”更能帮助安排工作，但不适合永久占据大量屏幕。
- 用户已经打开宠物，再增加独立悬浮球会显得拥挤。
- Codex 更新频繁，修改源码或安装包会增加失效和恢复成本。

## 3. 目标用户

首要用户：

- Windows 11 上使用 Codex 桌面客户端；
- 经常运行长任务或并发多个任务；
- 已经开启官方宠物；
- 希望低打扰地掌握用量状态。

非首要用户：

- 需要精确账单、组织级成本分摊或 API Key 消耗分析的团队；
- 不使用 Codex 桌面宠物的用户；
- 需要修改 Codex 主界面布局或完整换肤的用户。

## 4. 产品承诺

### 一眼可懂

用户不点击任何入口，在一秒内看懂最重要的剩余额度。

### 口径诚实

官方限额与本机 Token 活动必须视觉分组，并明确标注 Token 不是账单、也
不能直接换算额度。

### 安静陪伴

默认不弹通知、不抢焦点、不制造第二个悬浮物。宠物隐藏时底座立即隐藏，
程序留在托盘等待。

### 不碰官方文件

不修改 `app.asar`、安装目录、宠物包、认证文件或 Codex 设置。兼容失败
时隐藏和解释，不通过注入自救。

### 装饰服从信息

底座主题可以改变材质、颜色和气质，但不能改变指标位置、含义和可读性。

## 5. 信息架构

```text
常驻 Dock
  ├─ 官方周额度剩余（主指标）
  ├─ 本周本机 Token（辅指标）
  └─ 顶部进度光带（官方周额度）

点击详情
  ├─ Official limits
  │   ├─ 每个实际返回的额度窗口
  │   ├─ 剩余百分比
  │   └─ 重置时间
  ├─ Local activity
  │   ├─ 本周 Token 总量
  │   ├─ 非缓存输入
  │   ├─ 缓存比例
  │   ├─ 输出
  │   └─ 本机会话数
  └─ Data note
      ├─ 最近更新时间
      ├─ 数据来源
      └─ Token 口径说明

托盘
  ├─ Show details
  ├─ Refresh now
  ├─ Base theme
  ├─ Settings
  ├─ Diagnostics
  └─ Exit
```

## 6. 常驻 Dock

### 默认指标

| 位置 | 指标 | 原因 |
|---|---|---|
| 左侧 | `WEEK LEFT` | 产品的首要问题是额度是否够用 |
| 右侧 | `WEEK TOKENS` | 满足用户对本周活动量的持续感知 |
| 上沿 | 剩余额度光带 | 不增加第三组文字也能表达趋势 |

当服务端同时返回多个额度窗口时，左侧显示剩余百分比最低的一项，标题按
窗口切换为 `5H LEFT`、`DAY LEFT`、`WEEK LEFT` 或 `LIMIT LEFT`。

### 不常驻显示

以下信息只在详情中出现：

- 套餐名称；
- 精确重置日期与时间；
- 缓存输入、非缓存输入、输出；
- 会话数；
- reset credits；
- 数据来源说明。

这些内容对解释有价值，但不值得永久增加视觉噪声。

### 颜色语义

| 状态 | 颜色 | 行为 |
|---|---|---|
| 剩余 > 50% | 冷青色 | 正常 |
| 剩余 21–50% | 暖琥珀 | 提醒 |
| 剩余 ≤ 20% | 柔和红色 | 紧张 |
| Loading | 蓝灰色 | 保留位置，不闪烁 |
| Offline | 红灰色 | 显示 `!`，点击看原因 |

颜色不是唯一信息来源，数值和状态文字始终保留。

## 7. 详情面板

详情面板不是“小型数据大屏”，而是对 Dock 两个数字的解释层。

### 第一层：Official limits

优先展示服务端实际返回的限额窗口。每个窗口只包含：

- 名称；
- 剩余百分比；
- 一条进度；
- 本地时区重置时间。

不推测不存在的 5 小时、每日或周额度。

### 第二层：Local activity

展示本周本机会话活动：

- Total processed；
- Fresh input；
- Cached input ratio；
- Output；
- Sessions。

固定说明：

> Local activity includes cached input. It is not billing and does not
> determine the official remaining percentage.

### 第三层：来源和新鲜度

底部始终提供：

- `Official limit · Codex app-server`
- `Local activity · local token_count events`
- `Updated HH:mm:ss`

用户应该知道每个数字来自哪里，而不是因为 UI 好看就默认相信它们属于
同一口径。

## 8. 核心交互

### 左键点击 Dock

切换详情面板显示/隐藏。面板优先出现在 Dock 左侧，空间不足时自动切换到
右侧，并限制在当前显示器工作区内。

### 右键点击 Dock

打开与托盘一致的精简菜单：

- Refresh now；
- Base theme；
- Settings；
- Exit。

### 拖动宠物

Dock 作为一个整体跟随，不允许宠物和底座被单独拖开。快速移动时提升
跟踪频率，停止后自动降频。

### 切换宠物

Dock 重新发现当前宠物元素并居中，不继承旧宠物的尺寸和坐标。

### 隐藏宠物

Dock 与详情面板立即隐藏，不退出后台进程、不弹错误。再次打开宠物后自动
恢复。

## 9. 状态设计

### First run

一键接入的原则是“一次安装、零次绑定”：

1. 检测 Codex 桌面客户端；
2. 使用官方客户端现有登录状态检测 app-server；
3. 检测宠物；
4. 显示一次隐私说明；
5. 让用户选择默认主题；
6. 自动进入 Dock，不要求额外连接账户。

如果宠物未打开，显示托盘引导，不创建独立悬浮球。

完整流程见 [docs/one-click-onboarding.md](docs/one-click-onboarding.md)。

### Loading

底座保留固定尺寸，数值使用 `--`，不采用持续旋转动画。

### Offline

底座显示 `!`。点击后区分：

- Codex 未运行；
- 宠物未打开；
- app-server 超时；
- 登录不可用；
- 本机会话目录不可读。

错误不能全部归为“网络异常”。

### Update incompatibility

发现官方窗口但找不到宠物元素时：

1. 短时使用最后有效边界；
2. 尝试窗口锚点；
3. 持续失败则隐藏；
4. Diagnostics 提供非敏感环境信息。

## 10. 设置

### MVP 设置

- Base theme；
- Language：English / 简体中文；
- Launch at sign-in，一键安装默认开启，首次提示中说明可随时关闭；
- Show local Token activity；
- Reset position offsets；
- Open Theme Studio / Reload custom themes；
- Diagnostics。

### 后续设置

- 左右指标可选；
- 低额度提醒阈值；
- 刷新频率；
- 从 ZIP 导入安全主题包（Theme Studio 与本地 PNG + JSON 已支持）；
- Reduce motion。

提醒默认关闭，避免工具从低打扰仪表盘变成通知制造器。

### 与 Codex 一起出现

不修改 Codex 快捷方式，也不向官方进程注册插件。一键安装默认开启
`Launch at sign-in`，Pet Dock 随 Windows 登录进入低频待命；用户可在托盘
随时关闭：

- Codex 宠物未发现或隐藏时每 1 秒轻量发现一次，并暂停额度探测；
- Codex 和宠物出现：自动附着并开始刷新指标；
- Codex 退出或宠物隐藏：立即隐藏 Dock，继续在托盘等待；
- 无论用户从开始菜单、任务栏或更新后的路径启动 Codex，都无需重新绑定。

额度与 Token 自动刷新默认每 5 分钟一次；打开详情且数据超过 5 分钟，或
用户手动刷新时即时查询。后台不按秒扫描会话文件。

正式 `.NET` 版本使用同一产品行为；当前 PowerShell Preview 通过当前用户
`Run` 项实现，不需要管理员权限。

## 11. 主题

首发内置：

- **Holo Cyan 3D**：默认，和蓝色官方宠物最协调；
- **Holo Amber 3D**：更温暖，低亮环境更柔和；
- **Circuit Flat**：更小、更克制；
- **Princess Cradle**：象牙、粉色软垫、珍珠与香槟金；
- **Forest Rune**：古石、苔藓与青色符文；
- **Clockwork Brass**：黄铜、胡桃木与琥珀能量管；
- **Moon Lotus**：月白瓷、青玉与靛蓝月光；
- **Sakura Shrine**：朱漆、樱花与黄铜构件；
- **Iron Throne**：黑石阶、锻铁剑阵与暗红进度光；特殊 224×96 高背主题。

主题必须满足：

- 相同指标层级；
- 最小字号和对比度一致；
- 不在文字区域绘制高频纹理；
- 不改变错误/警告颜色含义；
- 素材可公开分发；
- 切换失败可回滚。

## 12. 视觉方向

方向：**克制的复古未来工业感**。

不是泛滥霓虹、玻璃拟态或数据大屏。主要材质是深海军蓝石墨，青色只用于
进度和能量接触点。宠物负责“可爱”，Dock 负责“可靠”；二者通过承托盘、
接触阴影和前后遮挡连接。

字体策略：

- 小标题：Bahnschrift，窄、清楚、像仪表铭牌；
- 数字：Bahnschrift SemiBold，数字宽度稳定；
- 详情正文：系统 UI 字体，优先可读性和本地化。

## 13. 隐私

- 默认无远程产品分析和行为遥测；
- 不读取 `auth.json`；
- 不输出账户邮箱；
- 不读取会话正文；
- 本地日志只记录时间、组件和错误消息；
- Diagnostics 输出前脱敏；
- 第三方主题不得包含脚本。

## 14. 版本边界

### Preview 0.1：验证产品核心

- 官方周额度常驻显示；
- 本周本机 Token；
- 点击详情；
- 宠物拖动、切换、隐藏恢复；
- 九款内置主题；
- 托盘切换主题；
- 本地错误日志。

### Beta 0.5：普通用户可安装

- .NET 8 单文件；
- `Setup.exe` 与便携 ZIP；
- 首次使用引导；
- 中英文；
- 设置页面；
- 无 Node.js/PowerShell 依赖；
- 多机器、混合 DPI 测试。

### Stable 1.0：可以长期维护

- 代码签名；
- 升级保留配置；
- 更新检查；
- 兼容性自检；
- 安全主题导入；
- 完整无障碍与 Reduce motion；
- 崩溃恢复和诊断包。

## 15. 成功标准

- 用户在 1 秒内回答“还剩多少额度”；
- 打开详情后能区分官方额度和本机 Token；
- 宠物拖动时肉眼无明显脱离；
- 切换宠物后 500ms 内重新贴合；
- 隐藏宠物不弹框，Dock 在 1 秒内消失；
- 空闲状态低 CPU 占用；
- Codex 更新不兼容时不修改官方文件；
- 新用户无需阅读命令行文档即可完成安装和首次启动。

## 16. 明确不做

- 不做完整 Codex 换肤；
- 不分发第三方宠物 IP 素材；
- 不提供 API 账单或组织成本管理；
- 不把本机 Token 转换成“剩余额度”；
- 不承诺绕过或增加官方额度；
- 不通过 CDP/DLL 注入获得更紧密的 DOM 集成；
- 不在 MVP 中建设在线主题商城。
